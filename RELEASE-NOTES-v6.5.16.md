# SIMS Manager v6.5.16

- GA4収益改善プロトタイプで確認した「記事本体とサイト共通要素の分離」の考え方をSBMへ移植。
- はてなブログの`<title>`末尾に付くブログ名をSEOタイトル保存時に除去し、記事固有のSEOタイトルだけを保持。
- 記事本文取得は`entry-content` / `post-content` / `article-body`等の本文コンテナを最優先し、ページ全体・記事ヘッダー・関連記事・フッターなどの混入を抑制。
- はてなブログで共通H1（ブログ名）が残るフォールバック経路にも除外処理を追加。
- キャッシュキーを更新し、旧抽出結果が残って今回の修正確認を妨げないようにした。
- GSC取得、改善ガイド判定、aWriter依頼文生成、aWriter Contractは変更なし。
