# SIMS Manager v5.22.3

## Operational-test synchronization

- Synchronized all repository and distribution `Code.gs` mirrors to the validated v5.22.3 runtime.
- v5.22.1: WAIT / MONITOR can start as a new monitoring cycle even when no prior improvement history exists. The standard effect-measurement schedule is 7日目・14日目・21日目・28日目（1週間ごと）.
- v5.22.2: Creator new-article registration requires the published URL and the About dialog now shows product identity and measurement guidance.
- v5.22.3: Removed the duplicate custom close button from the About dialog and retained only the shared dialog-footer close action.
- Synchronized `VERSION`, `PRODUCT_IDENTITY.json`, README files, distribution, release notes, and GitHub release metadata to v5.22.3.

## Scope

This repository synchronization does not introduce changes beyond the already validated v5.22.3 `Code.gs` runtime.
