# SIMS Manager v6.2.23

- 「記事情報を更新」は先にダイアログを表示し、記事DB点検を非同期で開始するよう変更。
- 点検中は処理内容とスピナーを表示し、無反応に見える時間を解消。
- 更新ボタンの `google.script.run` がprivate関数（末尾 `_`）を直接呼んでいた問題を修正し、公開Bridge経由へ変更。
- 更新中はボタン自体を「更新中…」＋スピナーへ変更し、数十秒〜数分かかる可能性を明示。
- ダイアログ内に独自の閉じる操作がある場合は共通Closeの二重追加を防止。
