# SIMS Manager v6.7.45 Release Notes

## 概要
主力のエース記事が大きく下落した場合に、通常の改善候補や90日再評価を待たず、原因確認を優先できる「エース急落」ルートを追加しました。

## エース急落の対象
- 日次GSC更新直前に `🏆 エース` だった記事、または更新後も `🏆 エース` の記事。
- `改善中`、`モニター中`、既に `今日の改善` で処理中の記事は二重Workflow防止のため除外。
- `✔️ 完了` は最終改善完了から14日間は急落による早期解除を行いません。

## 急落条件
直近28日と前28日を比較し、次のいずれかを満たす場合だけ急落とします。
1. 両期間の表示回数が50以上、平均順位が1.5位以上悪化し、クリックが20%以上かつ2クリック以上減少。
2. 前28日の表示回数が100以上、表示回数が30%以上減少し、クリックが20%以上かつ2クリック以上減少。
単一指標だけでは急落扱いにしません。

## 今日の改善
- `🚨 エース急落` は通常4カテゴリの各2件枠とは別枠です。
- 統合優先順位は `エース急落 → 経過観察終了 → 通常候補`。同一記事の重複表示は行いません。
- 固定8件上限には合わせません。

## 完了記事
急落条件を満たした完了エースだけ `未着手 / GSC急落再評価` に戻し、再評価予定日を解除します。通常の完了記事は従来どおり90日保護されます。

## aDoctor
「今日の改善」でエース急落を選び「改善内容を見る」を実行すると、記事を自動修正せずaDoctor精密診断へ進みます。

## 性能
監視対象エースが存在する場合だけ、サイト単位のpageデータを直近28日・前28日の2期間で一括取得します。記事ごとのGSC API逐次取得は行いません。
