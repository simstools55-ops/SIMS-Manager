# SIMS Manager v6.7.60 Release Notes

## 修正内容

4週観察終了後に記事管理が `🔄 再診処置` へ遷移しているにもかかわらず、「今日の改善」が0件になる問題を修正しました。

## 原因

`sbmObservationEndedTodayCandidates_()` が、記事管理の作業状態ではなく、同一記事の「最新の改善履歴」を起点に候補を作っていました。同一記事に別サイクルのより新しい履歴が存在すると、4週完了・再診対象の履歴が選ばれず、記事管理が `🔄 再診処置` でも候補から脱落していました。

## 対応

- 記事管理の `作業状態 = 🔄 再診処置` を候補生成の正本に変更。
- 改善履歴は、該当記事の完了済み・改善未完了サイクルから履歴ID等を補完する用途に限定。
- 履歴が取得できない旧データでも、ArticleIDと記事URLがあれば再診処置候補を生成。
- v6.7.58のキャッチアップ、4週判定、aDoctor/aWriter Workflowは変更なし。
