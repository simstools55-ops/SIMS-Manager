const fs=require('fs');const code=fs.readFileSync('apps-script/Code.gs','utf8');const checks=[['version',code.includes("const SBM_VERSION = '5.9.1';")],['h1 order',code.includes("'メインクエリ','H1タイトル','クリック数'")],['h1 fallback',code.includes("r['H1タイトル'] = r['H1タイトル'] || r['記事タイトル'] || ''")],['effect visible',code.includes('sh.showColumns(1, Math.min(11, sh.getMaxColumns()))')],['effect cols',code.includes("'改善前クリック':110,'現在クリック':110,'改善前表示回数':120,'現在表示回数':120,'判定':110")]];let ok=true;for(const [n,v] of checks){console.log((v?'PASS ':'FAIL ')+n);if(!v)ok=false;}process.exit(ok?0:1);
