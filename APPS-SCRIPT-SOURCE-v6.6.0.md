# SIMS Manager v6.6.0 Apps Script Source

v6.6.0以降、Full / StarterでCode.gsを差し替えません。

Apps Scriptへ使用する正本:
- `apps-script/Code.gs`
- `apps-script/appsscript.json`

EditionはSIMS License Centerのライセンス情報により実行時に切り替わります。
旧 `full` / `starter` 個別コード・Editionフォルダーは廃止しました。
