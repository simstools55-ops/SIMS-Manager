# SIMS Manager v6.6.69

## 変更内容
- v6.6.68の「記事管理を状態管理の正本」とする設計、および表示時の高速経路を維持。
- 日次処理STEP1で実測約11秒を要していた設定保存を監査。
- 15個の設定キーを個別 `sbmSetSetting_()` で保存していた処理を `sbmSetSettingsBatch_()` 1回へ統合。
- 保存する設定キー・値・意味は変更せず、Google SheetsへのI/O回数のみ削減。
- Search Console取得、URL正規化、記事状態参照、記事行生成、ソート、作業シート保存のロジックは変更なし。

## 確認項目
1. 日次処理が最後まで完了すること。
2. 処理プロファイルのSTEP1「終了:完了」で「設定保存」が大幅に短縮すること。
3. 「今日の改善」が数秒で表示され、候補内容がv6.6.68と同等であること。
4. 「未完了の作業を再開」が数秒で表示され、完了済み・重複案件が混入しないこと。
