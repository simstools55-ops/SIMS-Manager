# SIMS Manager v6.6.93

## 修正

- 「未完了の作業を再開」で `WRITER_REQUEST_READY` / `WRITER_IN_PROGRESS` の案件を選択した際、選択済みCaseをそのままaWriter再開画面へ復元するよう修正。
- v6.6.92では未完了一覧には表示できても、再開時にSite Doctor共通処置の再探索へ入り「再開できる未完了の作業は見つかりませんでした」となる経路があった。
- 高速化ロジック、候補抽出、日次処理、ランク判定には変更なし。

## 実運用試験

- 基準版 v6.6.92 の実運用試験で発見した再開不具合のみを最小修正。
- 次の確認対象：A000103相当の「aWriterへの依頼待ち」を未完了一覧から選択し、保存済みaWriter紹介状の画面へ直接復帰できること。
