# SIMS Manager v6.7.47 Release Notes

## 目的
記事管理を作業状態の正本に統一し、「今日の改善」シートを状態管理ではなく当日の作業キューとして整理します。

## 変更
- 作業状態に `🚨 急落` と `🔄 再診処置` を明示。
- 4回の経過観察終了後、改善効果が不十分な `REVIEW_REQUIRED` は `🔄 再診処置` へ遷移。
- 再診処置は既存 `EFFECT_AFTER_OBSERVATION` Workflowを利用してaDoctor再診へ進む。
- エース急落検知時は記事管理を `🚨 急落` にし、診断開始時に `✏️ 改善中` へ遷移。
- 通常改善は改善ナビ開始時に `✏️ 改善中` へ遷移。
- Doctor→Writer / Merge系の旧 `🛠️ 処置中` 書込みを `✏️ 改善中` へ統一。
- 日次処理で旧状態を移行：旧 `今日の改善` は最新改善履歴が測定完了かつ `REVIEW_REQUIRED` なら `再診処置`、それ以外は `未着手`。旧処置中系は `改善中`。

## 今日の改善
通常カテゴリ候補に加え、`🚨 急落` と `🔄 再診処置` を優先追加します。通常候補として表示されたこと自体では記事管理の作業状態を変更しません。
