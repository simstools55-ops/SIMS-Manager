# SIMS Manager v6.6.82 Release Notes

## STEP2 記事DB差分反映の高速化

v6.6.81の診断で、検索指標変化が0件でも「データ更新日」「最終取得日時」「最終確認日」が435件ずつ変更扱いになっていることを確認しました。

Google Sheetsでは書き込んだ日付文字列がDate型として読み戻される場合があります。従来は文字列化した表現を比較していたため、意味上は同じ日付・時刻でも差分と判定され、不要な再書込が発生していました。

v6.6.82では、データ更新日・最終確認日は暦日、最終取得日時は秒単位の時刻として比較します。実際に値が変わった場合だけ書き戻します。記事管理を状態管理の正本とする設計と各項目の意味は変更しません。
