# SIMS-Blog-Manager Product 5.10.0-RC8 Final Hotfix 9

## 健康診断排他制御・改善結果登録UX Hotfix

- 日次処理開始時の健康診断排他判定を修正。
  - `DoctorActiveHealthCheckId` に古い未完了Runが残っていても、それだけで「現在健康診断中」と判定しない。
  - `COMPLETED` と `RETRYABLE_ERROR` は日次処理を妨げない。
  - 実行中ステータスでも最終更新から30分を超えたRunは stale とみなし、再開用記録を保持したまま日次処理を許可する。
  - 直近30分以内に更新されている実行中Runのみ、従来どおり日次処理をブロックする。
- 通常の「改善結果を登録」ダイアログで、登録ボタン押下直後に処理中状態を明示。
  - ボタンを `登録中...` に変更して無効化。
  - 小さなスピナーと「記事管理・改善履歴・改善の推移を更新しています…」を表示。
  - 失敗時はボタンを復帰し、再実行可能にする。
- Doctor→Writerの処置結果登録でも同様にボタン表示を `登録中...` に変更し、二重登録を防止。

## 狙い

- 前日以前の未完了健康診断記録が残っただけで、通常の日次処理を永久に妨げる状態を解消する。
- Writer回答登録後の数秒間に「押せたか分からない」という不安をなくし、処理中であることを明確にする。
