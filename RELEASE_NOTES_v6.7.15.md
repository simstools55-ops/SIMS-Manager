# SIMS Manager v6.7.15 Release Notes

## 修正
- 「未完了の作業を再開」で、候補抽出Coreが完了しても待機ダイアログが残り続ける問題を修正。
- 待機ダイアログから非同期でCoreを呼び、Core側でも別ダイアログを開く二重UI方式を廃止。
- メニューからCoreを直接実行し、候補抽出完了後に再開候補／対象Workflow画面だけを表示する方式へ単純化。

## 影響範囲
- Workflow候補抽出、重複整理、Case判定、aDoctor再診結果受取ロジックは変更していません。
- 候補抽出中はSpreadsheetのトーストで処理中を案内します。
