# APPLY v5.14.9

1. Replace the current Apps Script `Code.gs` with `apps-script/Code.gs`.
2. Save the Apps Script project.
3. Reload the spreadsheet.
4. Confirm that selecting a second checkbox clears the first selection.
5. From `記事一覧`, check one article and run `4．選択記事の管理状態を変更`.
6. For Merge, confirm that completion allows either a 301 redirect or the no-redirect/search-exclusion route.
