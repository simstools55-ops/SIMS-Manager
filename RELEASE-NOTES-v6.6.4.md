# SIMS Manager v6.6.4

## 表示系3シートの高速化
- 改善の推移：毎回の全件日付検査・タイトル補正・全行書式設定を廃止。版更新後の初回だけ自己修復し、以後は保存済み表示を直接開く。
- 記事管理：毎回の全件タイトル正規化、全行スタイル、テーマ再描画を廃止。初回修復＋表示スタイルキャッシュへ変更。
- 改善履歴：毎回の全体テーマ再描画を廃止。既存の軽量View整形＋テーマキャッシュへ変更。
- 3シートとも showSheet / setActiveSheet は状態が変わる場合だけ実行。activate の重複呼出なし。
- 日次処理・診断・改善候補・履歴データ生成ロジックは変更なし。
- Code.gsコメントの旧バージョン番号表記を整理し、現行リリース表記以外は数値版名に依存しない説明へ統一。
