# SIMS Manager v5.21.44-dev

## 変更内容

- ダイアログの処理成功後、完了済みの実行ボタンを非表示にするUIルールを横断適用。
- aCreator新規記事登録、aWriter改善結果登録、aDoctor精密診断内のaWriter/aMerge結果登録、Site Doctor内のaWriter/aMerge結果登録・aMerge完了処理を対象。
- 次工程が既に表示されるダイアログでは、その次工程だけを残す。次工程がない場合は「閉じる」のみ残す。
- エラー時は従来どおり実行ボタンを再有効化し、再試行可能。
- 日次処理ロジックはv5.21.43から変更していない。

## 運用試験

開発正本のみ。正式distributionの確定は実運用試験収束後に行う。
