# SIMS Manager v6.6.87 Release Notes

## 目的
v6.6.86 の深掘り計測で特定した STEP3 の主要ボトルネックを、業務仕様を変えずに削減します。

## 変更内容
- 改善の推移「記事別判定ループ」: 記事管理の作業状態がすでに目的状態の場合、同じ値を再度シートへ書き込まないようにしました。
- 今日の改善「シート描画」: 日次再描画時の `clearFormat()` を廃止し、既存の書式を再利用します。内容と入力規則だけを初期化します。
- 今日の改善「設定保存」: 同一処理内での `DisplayedImprovementCount` の重複保存を省略します。

## 維持する仕様
- 今日の改善の候補選定、経過観察終了判定、記事ランク、Workflow、表示件数・内容は変更しません。
- Full / Starter は同一コードラインで維持します。

## 確認ポイント
日次処理後、処理プロファイルの `STEP3_EFFECT_COMPUTE_LOOP`、`STEP3_TODAY_SETTINGS_WRITE`、`STEP3_TODAY_SHEET_WRITE` と STEP3 合計時間を v6.6.86 と比較してください。
