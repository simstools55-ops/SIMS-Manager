# SIMS Manager v5.21.36-dev

## 修正
- 「改善の推移」に旧 `4回／4回` / `【測定完了】` 表示が残る既存シートを、最初に開いた時だけ自動再同期する自己修復を追加。
- v5.21.35導入前にaDoctor再診で追加経過観察へ移行済みの案件も、最新 `ACTIVE` 改善履歴を優先して表示する。
- 通常時は閲覧だけで全件再計算しない従来の高速表示を維持。
- 週次進捗は `n週／N週`、終了表示は `【経過観察終了】`、処置メニューは「経過観察終了後の処置を進める」に統一。

## 期待される既存データ修復
A000028のように旧サイクルが表示に残っていても、「改善の進捗」を開くと最新のaDoctor追加経過観察サイクルへ切り替わる。
