# SIMS Manager v6.7.35

## 修正内容

「未完了の作業を再開」実行時のUIだけを局所修正しました。確認中表示は右下toastではなく画面中央のmodeless待機ダイアログで表示し、候補探索完了後は既存の結果画面へ遷移します。

未完了案件が0件の場合は、中央ダイアログで次の結果を明示します。

> 未完了の作業はありません。現在、再開が必要な作業はありません。

## 非変更範囲

- 未完了候補の探索条件
- WorkflowState / Doctor Case / 通常改善の候補統合
- 候補のソートと既存の再開選択画面
- 高速化済みの一括読込・索引化処理
- 個別Workflowの再開処理

## 実運用確認

1. 未完了0件で中央に確認中表示が出ること。
2. 確認終了後、0件結果メッセージが中央表示されること。
3. 未完了1件以上では従来の再開選択画面が表示されること。
