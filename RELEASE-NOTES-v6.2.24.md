# SIMS Manager v6.2.24

## 変更点

- 日次処理 STEP1 で、実測メインクエリ未取得かつ表示実績がある記事だけを候補化。
- Search Console の `query × page` データを1回だけ一括取得し、候補URLの代表クエリをArticle DBへ補完。
- 既に実測メインクエリがある記事は上書きしない。推定値・未取得値だけを補完対象にする。
- 「記事情報を更新」からURL単位のSearch Console個別照会を廃止。
- 「記事情報を更新」は、Article DB内だけで直せるタイトルを先に補完し、必要な記事ページだけを最大8件ずつ `UrlFetchApp.fetchAll` で並列取得。
- 長い記事情報更新はクライアント側から小分け処理を自動継続し、Apps Script実行上限によるタイムアウトを回避。
- 記事情報点検はArticle DBのみを読み、Search Consoleや記事ページへアクセスしない。直近点検結果は短時間キャッシュして再表示を高速化。
- ダイアログ内の重複タイトルを削除し、「次回の日次処理で確認」「記事ページ確認対象」を明示。
- 日次処理完了画面に「実測メインクエリ補完」件数を追加。

## 互換性

- 既存の実測メインクエリ、記事タイトル、SEOタイトル、メタディスクリプションは保護。
- 初回セットアップの専用記事情報補完フローは維持。
- Full / Starter は同一v6.2.24ライン。Starter表示は v6.2.24-ST。
