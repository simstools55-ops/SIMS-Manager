# SIMS Manager v6.6.2

- v6.6.1で4区分×最大2件の選定ロジック自体は入っていたが、既存の保存済み「今日の改善」候補を固定表示する旧挙動により、更新直後は旧5件が残る問題を修正。
- 仕様変更後の初回「今日の改善」表示時だけ保存候補を再選定し、4区分×最大2件へ移行。
- 以後は従来どおり、その日の作業中の候補固定を維持。
- 高速差分更新でも各区分2件上限を維持。
- Code.gsコメントの旧バージョン番号プレフィックスを整理し、現行仕様コメントを追加。
- リポジトリ内のCode.gs / Code.base.gsを同期。

コメント整理件数: 362
