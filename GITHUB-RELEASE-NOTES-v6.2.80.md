# SIMS Manager v6.2.80

## v6.2.80 — カニバリ精密診断ダイアログ遷移修正

- 記事詳細の「カニバリ精密診断へ進む」で、次画面を開いた直後に親ダイアログが `host.close()` してしまう競合を修正。
- 追加診断では成功時に親側から close せず、aDoctor精密診断ダイアログを維持。
- 失敗時は元画面を閉じず、ボタンを再有効化してエラーを表示。
- aWriter結果登録直後のカニバリ精密診断導線も同じ方式へ統一。
- インデックス問題処置から追加診断へ進む導線も同じ競合を予防。
- 日次処理、未発芽判定、14日ゲート、記事情報更新、Writer/Doctor判定ロジック、Merge判定ロジックは変更なし。

