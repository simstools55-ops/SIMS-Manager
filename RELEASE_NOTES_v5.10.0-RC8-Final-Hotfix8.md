# SIMS-Blog-Manager Product 5.10.0-RC8 Final Hotfix 8

## 日次処理・今日の改善 UI/速度 Hotfix

- 日次処理中の内部シート「処理プロファイル」への書き込みで、利用者のアクティブシートを奪わないよう修正。
- 「処理プロファイル」を任意管理シートとして通常画面では非表示に統一。
- 「今日の改善」を開くたびに候補シート全体を再描画していた処理を差分判定へ変更。
  - 保存済み候補と表示URLが一致する場合は再描画しない。
  - 完了・モニター中の除外、表示件数変更、候補補充など実際に差分がある時だけ再描画する。
- 「今日の改善」再描画時の `sh.clear()` / 全1000行 `clearFormat()` 相当の広範囲処理を廃止し、最大10件の候補を扱う小さな作業領域だけを初期化するよう変更。
- 日次処理の候補選定ロジック、Doctor/Writer連携、健康診断、記事管理ロジックは変更していない。

## 狙い

- STEP 2/3〜3/3付近で「処理プロファイル」が見えるUI漏れを防止。
- 他の処理高速化後に相対的に目立っていた「今日の改善を開く」体感遅延を削減。
