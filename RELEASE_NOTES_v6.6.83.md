# SIMS Manager v6.6.83 Release Notes

## STEP2 日付診断の撤去と最終取得日時の一括更新

v6.6.81〜v6.6.82で使用した日付診断処理を撤去しました。診断は完了しているため、本番の日次処理では記事ごとの診断用日付パースを行いません。

「データ更新日」「最終確認日」は同一日なら不要な再書込を避ける方式を維持します。「最終取得日時」は日次取得のたびに更新される項目なので記事ごとの差分比較から外し、既存記事列を1回のsetValuesで一括反映します。

記事管理を状態管理の正本とする設計、GSC値、ランク判定、今日の改善候補、未完了判定の仕様は変更していません。既存の区間計測は維持します。
