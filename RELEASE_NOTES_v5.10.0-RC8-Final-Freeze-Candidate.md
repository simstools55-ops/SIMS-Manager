# SIMS-Blog-Manager Product 5.10.0-RC8 Final Freeze Candidate

## 目的
RC8の実機検証で残ったデータ品質・Doctor導線・履歴統合・状態管理の積み残しを解消し、Product 5.10.0正式版前のFreeze候補とする。

## 修正
- 記事一覧の利用者表示H1と記事タイトルを相互補完し、空欄を残さない。
- H1/記事タイトルが双方欠損している記事は、記事一覧表示時・日次処理時・Doctor一次検査前に上限付きで再取得する。
- 数字だけのURLスラッグはタイトルとして採用せず、未取得時は「タイトル取得待ち」と明示する。
- メインクエリは実クエリ／取得待ち／検索実績なしを区別し、表示用ラベルはDoctor/Writerのmain_queryへ渡さない。
- Doctor対応一覧は利用者導線から廃止し、Doctor経由の処置は改善履歴・改善の推移へ統合する。
- 改善方法は通常改善／Doctor→Writerを現在の実装で記録し、Creator/Mergeの将来ルートにも同じ列を利用できる構造を維持する。
- 記事ランクはDoctor処置で変更せず、作業状態を改善中→モニター中→完了の共通ワークフローで扱う。

## 移行
既存シートの削除・再作成は不要。旧Doctor対応一覧/診断状況シートは非表示のまま保持する。
