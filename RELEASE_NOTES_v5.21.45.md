# SIMS Manager v5.21.45-dev

## 日次処理の再設計

- 通常の日次処理では、記事DBの静的列を全件クリア・再書込しません。
- GSC更新で変化する列を狭い列ブロックだけで反映し、全行背景色の再描画を日次処理から除外しました。
- 記事DBが存在しない初回は100記事単位の分割構築へ切り替え、進捗カーソルをSettingsへ保存します。
- 初回構築は途中で止まっても保存済みカーソルから再開できます。
- STEP2が長時間化した場合は、記事DB差分反映後に候補選定を次のApps Script実行へ継続できます。
- タイトル/H1/メインクエリなどの外部補完は引き続き日次処理に含めません。
- 日次処理ログに「実セル変更」件数を追加しました。

## 方針

日次処理は「最新GSC値の反映・ランク再判定・今日の改善選定・必要な観察更新」に限定し、初期構築・補完・メンテナンス処理を分離します。
