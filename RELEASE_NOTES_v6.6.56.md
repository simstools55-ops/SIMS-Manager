# SIMS Manager v6.6.56

## 変更概要
未完了作業の開始時に、改善履歴で ACTIVE（モニター中）の ArticleID / URL を確認し、該当記事に残存する再開用一時データを物理削除してから未完了候補を生成します。

## 主な変更
- 「未完了の作業を再開」開始時にモニター中記事を照合。
- モニター中記事の `Doctor_Cases` を削除。
- 同記事の `Doctor_Workflow_State` を削除。
- 同記事の通常改善 WorkflowState も削除。
- aDoctor精密診断再開、共通処置再開にも同じクリーンアップを適用。
- モニター中でない記事だけを未完了候補として表示。

## 設計原則
未完了データは再開専用の一時データです。モニター中は作業完了後の効果測定フェーズなので、再開用データを保持しません。観察終了後に再診する場合は新しいWorkflowを開始します。
