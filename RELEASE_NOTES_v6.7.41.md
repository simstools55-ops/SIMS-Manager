# SIMS Manager v6.7.41 Release Notes

## 修正内容

日次処理で前日までに完了した記事が「今日の改善」へ再混入する問題を修正しました。

診断版 v6.7.40 で、STEP3 の `BeforeWrite` 時点ではすでに完了記事が通常候補へ戻っていること、さらに `completedOnSheet` が空であることを確認しました。原因は STEP2 が候補を再選定・再描画する際、STEP3 が完了行を読む前に「選択=完了」を失っていたことです。

v6.7.41 では STEP2 の再描画前に完了行の ArticleID/URL を退避し、その除外キーを候補選定と STEP3 再構築の両方へ引き継ぎます。これにより完了記事は当日の候補へ戻らず、空いた枠には既存ルールで適格な新候補が補充されます。

候補カテゴリ、優先順位、最大件数、GSC取得、経過観察終了候補の扱いは変更していません。
