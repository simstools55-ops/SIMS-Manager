# SIMS Manager v6.6.76

## 日次処理 STEP3 高速化

- v6.6.75の区間計測で「改善の推移: シート反映」が約20秒を使用していることを確認。
- 調査の結果、日次の改善の推移更新内部で `sbmSyncObservationEndedToToday_()` を実行した直後、STEP3本体でも同じ同期を再実行していた。
- dailyFast経路では内部同期を省略し、STEP3の「今日の改善整合」で1回だけ実行する。
- 手動更新時の即時同期は維持する。
- 記事管理を状態管理の正本とする設計、候補判定、経過観察終了判定は変更しない。

## 確認ポイント

処理プロファイルで `STEP3_EFFECT_WRITE` と `STEP3_改善の推移更新`、`STEP3_今日の改善整合`、`STEP3_EXIT` を比較する。
