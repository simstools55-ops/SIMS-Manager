# SIMS Manager v6.7.38 Release Notes

## 変更内容
- 日次処理 STEP3 の「今日の改善整合」で、既存の高速差分更新 `sbmRefreshTodayQueueFast_()` を実行するようにしました。
- 前日までに「完了」した記事、および現在「改善中」「モニター中」「完了」の記事を「今日の改善」から除外します。
- 除外で空いた枠は、既存の `sbmSelectTodayRecommendations_()` を使い、選択可能な候補がある範囲だけ補充します。
- 経過観察終了から「今日の改善」へ戻る既存フローは維持します。
- 候補カテゴリ、優先順位、各カテゴリ最大2件、全体上限などの候補選定仕様は変更していません。

## 修正範囲
日次処理から「今日の改善」へつなぐ最終整合処理のみの局所修正です。Search Console取得、記事DB差分更新、候補スコアリング本体、改善ナビ、未完了再開ロジックには変更を加えていません。
