# SIMS Manager v6.7.50 Release Notes

- `sbmHomeLayoutNeedsRebuild_()` の署名セルが旧配置を参照していた不整合を修正しました。
- 現行Homeではモニター見出しがA16、判定群見出しがA17/D17/H17にあるため、署名判定を現行配置へ同期しました。
- 正常なHomeでは `sbmOpenHome()` が再構築を行わず、画面遷移だけで終了します。これにより標準配色が一時表示されてから選択テーマへ戻る現象と不要な待ち時間を解消します。
- v6.7.48の孤立改善中整合、およびv6.7.49のprofiler修正には変更を加えていません。
