# SIMS Manager v6.7.42 Release Notes

## 修正内容

### 今日の改善：旧完了行の表示残骸を除去
`sbmPrepareTodayImprovementSheetFast_()` で旧データの値・入力規則に加え、使用範囲の背景色、文字色、取り消し線、太字を初期化します。これにより、記事データが消えた後も空行に「完了」表示の見た目だけが残る状態を防ぎます。

### 今日の改善：完了除外後の候補補充を修正
前版では通常候補を各区分最大2件まで選んだ後に前日完了記事を除外していたため、除外で空いた枠に同区分の次点候補が入りませんでした。v6.7.42では `sbmSelectTodayRecommendations_(exclusions)` が候補プール段階で完了除外キーを適用し、その後に各区分最大2件を選びます。

## 変更しない仕様
- 4区分から最大2件ずつ、最大8件
- 候補不足の区分を別区分で穴埋めしない
- 順位・CTR・表示回数・記事ランク等の評価基準
- 経過観察終了候補の追加表示
- GSC取得ロジック

## 実運用確認
日次処理後、「今日の改善」で空行に残る完了表示が消えること、前日完了を除外した同区分に次点候補が存在する場合は補充されることを確認対象とします。
