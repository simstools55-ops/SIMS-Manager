# SIMS Manager v6.7.5 Release Notes

## aWriter改善結果登録の実測ボトルネックを除去

v6.7.3 の実運用計測では登録全体 37.20 秒のうち、利用者向けシートの同期表示整形が 19.02 秒を占め、最大のボトルネックでした。

v6.7.5 では、aWriter改善結果登録の成立に不要な `sbmFinishUserSheetPresentation_({article:true,effect:true,history:true})` を同期登録経路から外しました。記事管理・改善の推移・改善履歴のデータ登録、Case保存、follow-up判定、未完了整理は変更していません。

各シートは表示時の軽量整形経路を持つため、登録完了時に3シートを再装飾する必要はありません。

区間計測は1回だけ継続し、07区間がほぼ0秒になり、総登録時間が短縮することを確認します。確認後は計測コードを撤去します。
