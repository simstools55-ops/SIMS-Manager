# SIMS Manager v6.6.46 Release Notes

## 日次処理ダイアログ起動の高速化

メニューから日次処理を呼び出した際、ダイアログ表示前に行っていたSettings・サイト健康診断Run・当日実施状態の確認を、ダイアログ表示後の非同期事前確認へ移動しました。

### 変更内容
- メニュークリック直後は軽量なダイアログ生成だけを行います。
- ダイアログ表示後に `sbmGetDailyDialogPreflight()` を1回呼び出します。
- Settingsは `sbmGetSettingsMap_()` で1回だけ読み、複数キーの反復シートI/Oを廃止しました。
- サイト健康診断RunはActive IDがある場合だけ確認します。
- 事前確認時間をログへ記録します。
- STEP1〜3の日次処理本体には変更を加えていません。
