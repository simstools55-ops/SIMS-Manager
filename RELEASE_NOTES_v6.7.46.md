# SIMS Manager v6.7.46 Release Notes

## 変更概要
「今日の改善」の選択列に表示していた `完了` を `終了` へ変更し、今日の改善作業の終了と、記事管理の改善サイクル全体の `✔️ 完了` を明確に区別しました。

## 互換性
- 旧版の「今日の改善」に残っている `完了` は読み取り時に `終了` と同等に扱います。
- 新規書込み・表示同期は `終了` を使用します。
- 記事管理の `✔️ 完了`、改善履歴・改善の推移の `完了` は変更しません。

## 影響範囲
- 今日の改善の終了済み行の退避・再構築・高速更新・クリーンアップ・表示同期・チェックボックスUI。
- 改善結果登録後の利用者向けメッセージ。
- エース急落監視、90日再評価、経過観察終了の判定ロジックは変更しません。
