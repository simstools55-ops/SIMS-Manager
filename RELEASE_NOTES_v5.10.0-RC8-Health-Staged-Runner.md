# SIMS-Blog-Manager Product 5.10.0-RC8 Health Staged Runner

## Purpose
ブログ健康診断を、利用者の1回の操作で最後まで完了できる分割STEP方式へ変更。

## Changes
- 健康診断を1つの長時間Apps Script実行から、複数の `google.script.run` STEPへ分割。
- 180日集計、前半90日、後半90日、直近28日、その前28日、健康状態判定を順次自動実行。
- STEPごとに健康診断Run状態とスナップショットを保存。
- Apps Scriptの実行時間上限等でSTEPが中断した場合、保存済み工程から自動再開可能。
- 処理中はHome/候補/診断書へ画面遷移せず、進捗ダイアログだけを表示。
- 完了後に `Doctor_健康診断書` を自動表示。
- 利用者向け進捗表示の「一次診断」を「健康状態判定」へ変更。
- 既存の健康診断スナップショット、Doctor連携契約、候補選定ロジックは維持。

## User workflow
1. SIMS Doctor → `1．ブログ健康診断を実行`
2. 開始確認でOK
3. 以後は自動進行
4. 完了後、健康診断書を表示

通常は再度メニューを選ぶ必要はありません。真のエラー時のみ、ダイアログ内の「続きから再開」を利用できます。
