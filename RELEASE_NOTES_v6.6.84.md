# SIMS Manager v6.6.84 Release Notes

## 目的
STEP2の `TODAY_WRITE` とSTEP3の「今日の改善整合」で共通して発生していた再描画コストを削減します。

## 変更
- 既存の「今日の改善」シートのヘッダー構造が正常なら、全面レイアウト再構築を行わず使用中のデータ範囲だけをクリアします。
- 候補行のチェックボックスは書込時に一括設定済みのため、直後の `sbmApplySelectionUi_()` を省略します。
- `sbmWriteTodayRecommendations_()` の強制 `SpreadsheetApp.flush()` を削除します。
- シートが未作成・ヘッダー不整合の場合は従来の完全構築へフォールバックします。

## 維持する仕様
記事管理を状態管理の正本とする設計、通常候補と経過観察終了候補の統合、候補判定、作業状態遷移は変更しません。

## 確認対象
前回実測の `STEP2_TODAY_WRITE` 約8秒、`STEP3_今日の改善整合` 約23秒、および `STEP3_EXIT` 約91秒と比較します。
