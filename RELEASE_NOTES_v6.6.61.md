# SIMS Manager v6.6.61

- 経過観察4回完了時、再確認が必要な記事は記事管理の作業状態を「モニター中」から「今日の改善」へ変更してから改善の推移の現役一覧から外す。
- 今日の改善の経過観察終了候補は、過去の4回測定履歴ではなく現在の作業状態が「今日の改善」の記事だけを表示する。後処置済みの過去案件を復活させない。
- 今日の改善シートに ArticleID 列を追加して表示する。記事URL・候補IDは引き続き内部列として非表示。
- 通常の日次改善候補の状態遷移は変更しない。

## 既存 v6.6.59/v6.6.60 データについて
v6.6.59/v6.6.60で既に改善の推移から削除された行は、削除前の状態情報が保存されていないため自動推測しない。誤って過去案件を復活させることを避けるため、現在状態を正本とする。
