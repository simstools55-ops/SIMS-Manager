# SIMS-Blog-Manager Product 5.10.0-RC7

## Human View Redesign & Workflow Status Cleanup

実機運用で確認された可読性とワークフロー状態管理の課題を改善したRCです。

### 主な変更

- `Doctor_精密診断紹介状` を利用者向け5列（選択 / 優先 / 記事タイトル / 診断理由 / 状態）へ再設計。
- ArticleID、URL、診断予定、180日指標は機械参照列として保持し、通常表示では非表示。
- Doctor/Writer処置完了後の記事は精密診断紹介状でグレーアウトし、再選択不可。
- Doctor紹介によるWriter処置結果を通常の改善履歴・効果測定へ接続。記事ランクは変更せず、作業状態だけ `👀 モニター中` に遷移。
- `Doctor連携状態` を `Doctor診断状況` へ刷新。記事ごとの最新Case状態、次にやること、再診予定日をHuman Viewで表示。
- 上部の独立した `結果登録` メニューを廃止。結果登録は各ワークフロー内で実施。
- 順番に操作する主要メニュー項目へ番号表示を追加。
- 健康診断書の本文列を拡張し、Doctor所見を読みやすく調整。
- H1/記事タイトル空欄は記事ランクに依存せず順次補完。表示実績があるのにメインクエリが空欄の記事も順次補完。

### Workflow

`Doctor → SBM → Writer → SBM → モニタリング` の結果登録を通常改善と同じ測定基盤へ統合しました。

### Compatibility

- Shared Editorial Knowledge: 3.5.0
- 既存の結果登録関数は後方互換のため保持しますが、独立メニューからは外しています。
