# SIMS Manager v5.21.35-dev

## 改善の推移・経過観察表示の修正

- aDoctor再診で WAIT / MONITOR（追加経過観察）となった直後に「改善の推移」を即時再生成するよう修正。
- 同一記事に旧 REVIEW_REQUIRED と新 ACTIVE サイクルが併存する場合、最新の現役サイクルだけを表示。
- 「測定回数」列の表示値を `1回／4回` から `1週／4週` へ変更。列名は従来どおり「測定回数」を維持。
- 観察期間終了時の表示を `【測定完了】` から `【経過観察終了】` へ統一。
- メニューを `3．経過観察終了後の処置を進める` に統一。
- 旧データの `n回／n回`、`測定完了` も終了判定できる後方互換を維持。

## 状態

運用試験用DEV。正式配布版ではありません。
