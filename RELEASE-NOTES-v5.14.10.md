# SIMS-Blog-Manager v5.14.10

## Fixes

- 記事管理状態の登録時に `今日の改善`、`改善の推移`、`Home` を連続再構築していた処理を廃止し、Spreadsheetサービスのタイムアウトを回避。
- 管理対象外にした記事は、表示中の `今日の改善` と `Doctor_精密診断候補` から必要な行だけ即時除外。
- 記事管理状態ダイアログは独自の「閉じる」ボタンを持つため、共通閉じるボタンの自動追加を停止し、二重表示を解消。
- 正本である記事管理の状態を先に確実に保存し、派生ビュー全体の再評価は次回の日次処理へ委譲。
- 現行バージョン表記を v5.14.10 に同期。
