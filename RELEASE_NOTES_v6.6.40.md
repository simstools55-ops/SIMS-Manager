# SIMS Manager v6.6.40

- 「0．HOME画面を開く」を画面遷移専用に変更。
- 既存HOMEが正常な場合は `sbmRefreshHome_()` を実行せず、現在のシート書式・表示テーマをそのまま維持してHOMEへ移動。
- HOMEが存在しない、またはバージョン／レイアウト更新が必要な場合だけ再構築し、その場合は現在の表示テーマを適用してから表示。
- これにより、モノクロ表示でHOMEを開いた際に一時的に標準配色へ戻ってからモノクロへ戻るちらつきを解消。
- データ更新・診断・効果測定ロジックは変更なし。
- 基準正本はv6.6.36。破棄扱いのv6.6.37 / v6.6.38の変更は含めない。v6.6.39の「今日の作業 0〜3」整理は維持。
